[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "alhikmah-llms"
version = "0.1.0"
description = "Helper utilities for the Hands-on LLMs for SWEs course"
requires-python = ">=3.10"
dependencies = ["torch>=2.0"]
